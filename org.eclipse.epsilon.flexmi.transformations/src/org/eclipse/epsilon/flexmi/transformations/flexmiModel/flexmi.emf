@namespace(uri="flexmi", prefix="")
package flexmiModel;

class FlexmiModel {
	attr String nsuri;
	attr String[*] imports;
	val Tag[*] tags;
}

class Tag {
	attr String name;
	val Attribute[*] attributes;
	val Tag[*] tags;
}

class Attribute {
	attr String name;
	attr String value;
}
